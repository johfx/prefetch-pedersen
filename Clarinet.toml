[project]
name = "prefetch-pedersen"
authors = ["Your Name <your.email@example.com>"]
description = "A decentralized identity management platform with secure, sovereign identity verification"
telemetry = false
requirements = []
[contracts.pedersen-identity]
path = "contracts/pedersen-identity.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
